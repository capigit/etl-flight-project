import requests
import pandas as pd
import sqlite3
import time
import gspread
from google.oauth2.service_account import Credentials
from datetime import datetime


# (archived) original ETL script
"""
Ce fichier est une version ancienne/alternative du pipeline.
La logique principale et la maintenance doivent se faire via `main.py` et `src/`.
"""

# ... (contenu archivé) ...
